[package]
name = "mcp23017-driver"
version = "0.1.0"
edition = "2021"
description = "An `embedded-hal` based Rust driver for the Microchip MCP23017 IO expander."
license = "MIT"

[dependencies]
embedded-hal = "1.0.0"
embedded-hal-bus = "0.2.0"
